version: '3.4'

services:
    java_app:
        build: ./java_app
        depends_on:
            - cplex
    cplex:
        build: ./cplex

    docplex:
        build: ./docplex
        depends_on: 
            - cplex
